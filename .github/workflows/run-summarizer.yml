name: Run Summarizer Pipeline

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  run-summarizer:
    runs-on: ubuntu-latest
    steps:
      - name: Call Supabase Batch Summarizer
        run: |
          curl -X POST "${{ secrets.SUPA_URL }}/functions/v1/pipeline-summarize-articles-batch" \
            -H "Authorization: Bearer ${{ secrets.SUPA_SECRET_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d '{}'
