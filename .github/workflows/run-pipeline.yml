name: Run Article Pipeline (Every Minute)

on:
  schedule:
    - cron: 
  workflow_dispatch:

jobs:
  run-pipeline:
    runs-on: ubuntu-latest

    steps:
      - name: Call Supabase Function
        run: |
          curl -X POST \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Content-Type: application/json" \
            https://kujkheijwbxfqixxvvpt.supabase.co/functions/v1/process-articles-batch
